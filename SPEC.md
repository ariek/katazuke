# 片付けクエスト 仕様書 v0.1

現実の部屋を片付けるためのブラウザゲーム。片付けタスクを「クエスト」として登録し、こなすと経験値が貯まり、画面上の部屋がきれいになっていく。

## 1. 前提

- 利用者は自分ひとり。プロフィール切り替えは不要。
- 動作環境はスマホのブラウザを主とし、PCでも使える。
- サーバーなし。データはすべて `localStorage` に保存する。
- 素の HTML / CSS / JavaScript で作り、ビルド工程なし。GitHub Pages で `main` ブランチのルートを公開する。
- 見た目はやわらかい手描き風（パステル調、少しラフな線）。

## 2. 画面構成

単一ページで、下部タブで5画面を切り替える。

| タブ | 内容 |
| --- | --- |
| 部屋 | ホーム画面。エリアごとのカードを並べ、各カードの絵に散らかり具合が反映される。上部にレベル、経験値バー、ストリークを表示。 |
| クエスト | 先頭に「いまやる1つ」のカード、その下にタスク一覧（エリアで絞り込み可）。完了ボタンで達成。 |
| タイマー | 片付けタイマー。タイマー中に達成したタスクはボーナス付き。 |
| 記録 | カレンダー（月表示）と週間サマリー。 |
| 設定 | エリア編集、データのエクスポート/インポート、初期化。 |

## 3. エリア

部屋を「エリア」に分割し、タスクはいずれかのエリアに属する。

- エリアは追加・名前変更・絵の種類の変更・並べ替え・削除ができる（設定画面）。並べ替えは ▲▼ ボタンで行う。
- 初期エリア案: 机、床、ベッドまわり、クローゼット、キッチン、洗面所・風呂、玄関。
- 削除時は所属タスクをまとめて削除する（確認ダイアログあり）。

## 4. タスク（クエスト）

現実の片付け内容をユーザーが自由に登録する。

### 4.1 項目

| 項目 | 説明 |
| --- | --- |
| タイトル | 例「机の上の書類を仕分ける」 |
| エリア | 所属エリア |
| 難易度 | ★1 / ★2 / ★3。目安は 5分 / 15分 / 30分以上 |
| 繰り返し | なし / 毎日 / 毎週 / N日ごと |
| 期限 | 任意。繰り返しなしのタスクに「いつまでに」を付けられる。繰り返しありのタスクは完了時に自動で次回期限が入る |
| メモ | 任意 |

### 4.2 状態

- 繰り返しなしのタスクは、完了すると「達成済み」になり一覧から消える（記録には残る）。
- 繰り返しありのタスクは、完了後に次回期限が設定され、期限が来ると再び「やること」として表示される。
- 期限を過ぎたタスクは「期限切れ」として強調表示する。
- 繰り返しなしで期限も未設定のタスクは、常に「やること」として表示され、きれい度の計算では期限内として扱う。

### 4.3 いまやる1つ

一覧から選ぶ手間をなくし、1つずつ片付ける形にする。

- クエスト画面の先頭に「いまやる」カードを1枚だけ大きく表示し、残りは「ほかのやること」として折りたたむ。
- 「いまやる」は次の順で自動的に決める: 当日「あとで」にしたものを除き、期限切れ → 今日 → 期限なしの順、同じなら難易度の低い順、さらに同じなら期限の早い順。
- 完了すると次の1つが自動で出る。
- 「あとで」ボタンでその日の末尾に回せる。全部を「あとで」にした場合は、先送りが早かったものから順に戻ってくる。「あとで」は日付が変わると解除される。残り1件のときは押せない。
- タイマー画面にも同じ「いまやる」カードを表示し、タイマー中に完了できるようにする。

### 4.4 操作

- 追加、編集、削除、完了、完了の取り消し（当日分のみ）。
- 完了時は経験値の獲得演出を出す: ボタンから紙吹雪が飛び、「+XP」が浮かび上がり、ヘッダーの経験値バーが光る。タイマー中はひとまわり派手にする。
- レベルアップ時は全画面のお祝い（新しいレベルと称号、紙吹雪）を出し、タップか3秒で閉じる。
- OS の「視差効果を減らす」設定が有効なときは、紙吹雪などの動きを省く。

## 5. ゲームルール

### 5.1 経験値とレベル

以下の数値はすべて仮置き。実際に使いながら調整する。

| 難易度 | 経験値 |
| --- | --- |
| ★1 | 10 |
| ★2 | 25 |
| ★3 | 50 |

- タイマー中の完了は 1.5 倍。
- 登録ボーナス: クエストを追加すると +1 XP、削除すると -1 XP（エリア削除でまとめて消える場合も1件ごと）。1日の上限はなし。減らすときは、そのレベルの開始値より下には減らない（レベルダウンしない）。記録画面のカレンダーやストリークには数えない。演出は完了より控えめ（小さな札のみ、紙吹雪なし）。
- レベルアップに必要な経験値は `100 × 現在レベル`。累積ではなく、レベルごとにゲージがリセットされる。
- レベルに応じて称号を付ける。

| レベル | 称号 |
| --- | --- |
| 1 | 見習い |
| 3 | 片付け見習い |
| 5 | 片付け係 |
| 8 | 整理上手 |
| 10 | 整理の達人 |
| 15 | 片付けマスター |

### 5.2 ストリーク

- 1日に1つ以上タスクを完了すると、その日は「達成日」。
- 達成日が連続した日数をストリークとして表示する。
- 日付の切り替わりは端末のローカル時刻の 0時。
- 1日空くとストリークは 0 に戻る。救済措置は v0.1 では入れない。

### 5.3 部屋の散らかり具合

各エリアの「きれい度」を、所属タスクの状態から自動で決める。

- きれい度 = 期限内のタスク数 ÷ 有効なタスク数（繰り返しなしで達成済みのものは除外）。
- タスクが1つもないエリアは「きれい」扱い。
- 3段階に丸めて絵に反映する。

| きれい度 | 表示 |
| --- | --- |
| 80% 以上 | きれい |
| 40% 以上 | ふつう |
| 40% 未満 | 散らかり |

期限が過ぎるほど散らかっていくので、放置すると絵が自然に元に戻る（リバウンド）。

## 6. 部屋のビジュアル

- エリアごとに1枚のカードを表示し、カードの中にそのエリアの絵を描く。
- 絵はインライン SVG。エリアの種類ごとに 3 段階の絵（散らかり / ふつう / きれい）を用意し、きれい度で切り替える。
- 手描き風にするため、線は少し揺らし、塗りはパステル調。
- カードにはエリア名、きれい度、期限が来ているタスク数を表示する。
- カードをタップすると、そのエリアのタスク一覧に移動する。
- エリアには「絵の種類」を設定する（机、床、ベッド、クローゼット、キッチン、洗面所、玄関、棚）。新しいエリアを作るときはこの中から選び、迷ったら「棚」を使う。
- カードはエリアの並び順で表示し、スマホでは1列、PCでは2〜3列に並べる。

## 7. 片付けタイマー

- プリセット: 5分 / 10分 / 15分 / 25分。
- 開始するとカウントダウン。タイマー画面にいなくても継続する（開始時刻を保存して差分で計算）。
- タイマー中に完了したタスクは経験値 1.5 倍。
- 終了時に音とバイブレーション（対応端末のみ）で通知する。
- タイマー中はヘッダーに残り時間を小さく表示し、どの画面からでも分かるようにする。タップするとタイマー画面に移動する。
- 途中でやめることもできる（確認あり）。その場合も達成数は記録するが「途中終了」と表示する。
- アプリを開いたときに終了時刻を過ぎていたら、音を鳴らさずに終了扱いにする。

## 8. 記録

- 上部に集計カード: 連続日数、最長連続、達成数の合計、獲得経験値の合計。
- 月カレンダー。達成日はマスに色を付け、完了数に応じて濃くする（1件 / 2〜3件 / 4〜5件 / 6件以上の4段階）。前後の月に移動でき、「今日」ボタンで戻る。
- カレンダーの日をタップすると、その日に達成したクエストの一覧（時刻、タイトル、エリア、経験値、タイマー中かどうか）を表示する。初期選択は今日。
- 週間サマリー: 今週（月曜始まり）の完了数、獲得経験値、活動日数、エリア別の完了数（横棒）。
- 削除されたクエストの記録は「（削除されたクエスト）」として残す。

## 9. データ保存

### 9.1 保存先

- `localStorage` のキー `katazuke.v1` に JSON を1つ保存する。
- 変更のたびに保存する。

### 9.2 データ構造

```json
{
  "version": 1,
  "player": { "xp": 0, "level": 1, "bestStreak": 0 },
  "areas": [
    { "id": "a1", "name": "机", "kind": "desk", "order": 0 }
  ],
  "tasks": [
    {
      "id": "t1",
      "areaId": "a1",
      "title": "書類を仕分ける",
      "difficulty": 2,
      "repeat": { "type": "days", "every": 3 },
      "note": "",
      "deadline": null,
      "deferredAt": null,
      "lastDoneAt": "2026-09-09T11:00:00+09:00",
      "dueAt": "2026-09-12T00:00:00+09:00",
      "done": false,
      "createdAt": "2026-09-09T10:00:00+09:00"
    }
  ],
  "logs": [
    {
      "taskId": "t1",
      "doneAt": "2026-09-09T11:00:00+09:00",
      "xp": 25,
      "inTimer": false,
      "prev": { "done": false, "lastDoneAt": null, "dueAt": null }
    }
  ],
  "timer": {
    "startedAt": null,
    "durationSec": 0,
    "lastResult": { "durationSec": 900, "finished": true, "count": 2, "xp": 52 }
  },
  "sample": false
}
```

- `player.xp` は累計経験値。`player.level` は保存時に `xp` から計算して入れる（表示用。読み込み時は `xp` を正とする）。
- `sample` は初回起動のサンプルデータが入ったままかどうか。設定画面の「サンプルを消す」ボタンの表示に使う。

- `repeat.type` は `none` / `daily` / `weekly` / `days`。
- `deadline` は繰り返しなしのタスクに任意で付ける期限。`dueAt` は繰り返しタスクの次回期限で、どちらも未設定なら `null`。
- `timer.lastResult` は直前のタイマーの結果（長さ、最後まで走ったか、達成数、獲得経験値）。タイマー画面に表示する。
- `deferredAt` は「あとで」を押した時刻。日付が今日のときだけ有効。
- `logs[].prev` は完了前のタスク状態。当日中の取り消しで元に戻すために使う。
- 繰り返しタスクの次回期限は、完了日の翌日（毎週なら7日後、N日ごとならN日後）の 0:00 にそろえる。
- 任意の期限（`deadline`）はその日の 23:59:59 とする。
- `areas[].kind` は絵の種類。`desk` / `floor` / `bed` / `closet` / `kitchen` / `bath` / `entrance` / `shelf`。
- 日付は ISO 8601 文字列で保存する。

### 9.3 エクスポート / インポート

- エクスポート: JSON をテキストエリアに表示してコピーできるようにする。ファイルダウンロードも用意する。
- インポート: JSON を貼り付けるか、ファイルを選択して読み込む。読み込み前に現在のデータを上書きしてよいか確認する。
- `version` が異なる場合は変換処理を通す。

### 9.4 初期化

設定画面から全データを削除できる。二重確認する。削除後は初期エリア（机、床、ベッドまわり、クローゼット、キッチン、洗面所・風呂、玄関）だけがある空の状態になる。

## 10. 初回起動

- サンプルのエリアとタスクを数件入れた状態で始める。
- 「サンプルを消して自分で登録する」ボタンを設定画面に用意する。押すとクエスト、記録、レベルを消し、エリアは残す。

## 11. 非機能

- スマホ縦画面を基準に設計し、PC では中央寄せの幅固定で表示する。
- オフラインでも開けるよう、外部ライブラリは使わない。フォントは Google Fonts の丸ゴシック（Zen Maru Gothic）を1つだけ使い、読めないときはシステムの丸ゴシックにフォールバックする。
- 外部への通信はしない。

## 12. ファイル構成

```
index.html      画面の骨組み
style.css       スタイル
app.js          状態管理、保存、画面切り替え
game.js         経験値、レベル、ストリーク、きれい度の計算
room.js         部屋の SVG とエリアの切り替え
quests.js       クエスト画面（一覧、追加・編集・削除、完了と取り消し）
timer.js        タイマー
effects.js      完了とレベルアップの演出
settings.js     設定画面（エリア編集、エクスポート/インポート、サンプル削除、初期化）
log.js          記録画面（カレンダー、日ごとの記録、週間サマリー）
dev/gallery.html エリアの絵を全種類・全段階まとめて確認する開発用ページ
.claude/launch.json ローカル確認用サーバーの起動設定（Claude Code 用）
README.md       概要、動かし方、公開手順
SPEC.md         この仕様書
CONTRIBUTING.md ブランチとコミットのルール
CLAUDE.md       Claude Code 向けの作業方針
```

スクリプトはビルドせずにそのまま読み込む。読み込み順は `game.js` → `room.js` → `quests.js` → `timer.js` → `settings.js` → `log.js` → `effects.js` → `app.js` で、`app.js` が起動処理を持つ。

## 13. 今後の候補（v0.2 以降）

- コインと装飾ショップ（観葉植物やポスターで部屋を飾る）。
- ストリークの救済（月1回まで休んでも途切れない）。
- PWA 化してホーム画面に追加できるようにする。
- タスクのテンプレート（「引っ越し後」「大掃除」など）。

## 14. 検討事項（未実装）

単一タスクで進めやすくするための案。「いまやる1つ」を使ってみてから判断する。

- **今日の順番を手動で決める** … 「やること」の中で ▲▼ やドラッグで順番を変える。全体の並べ替えより、朝に3つほど選んで並べる「今日のクエスト」欄の方が軽い。
- **クエストの分割支援** … 追加・編集シートに「分割」ボタンを付け、1つのクエストを複数の小さなクエストに変換する。★3を登録しようとしたら「5分単位に分けますか？」と促す。チェックリスト方式は小項目に経験値が付かず達成感が薄いので、独立した小さなクエストに分ける方針。
- **「次の1つを引く」演出** … カードをめくるように次の1つが出る。少しランダム性を入れて飽きにくくする。

## 15. 決定事項の記録

- 2026-09-09: 部屋の絵は「エリアごとのカード」形式にする。
- 2026-09-09: 繰り返しなしのタスクにも任意で期限を付けられるようにする。
- 2026-09-09: 経験値やレベルアップの必要量は仮の数値で作り、使いながら調整する。
- 2026-09-09: 単一タスクで進めるため「いまやる1つ」モードと「あとで」を入れる。手動の並べ替え、分割支援、カードを引く演出は検討事項として保留。
- 2026-09-09: クエストの追加で +1 XP、削除で -1 XP を入れる。上限なし、記録には数えない、演出は控えめ。
