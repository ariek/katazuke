# katazuke

部屋の片付けを続けるためのブラウザゲーム。素の HTML/CSS/JS、localStorage 保存、GitHub Pages で公開。

- 仕様は `docs/SPEC.md`。仕様を変えるときはコードより先にここを直す。
- ブランチ運用とコミットメッセージの形式は `docs/CONTRIBUTING.md` に従う。
- 公開する本体は `public/`、ドキュメントは `docs/`、開発用は `dev/`。GitHub Actions が `public/` だけを GitHub Pages に配信する。
- 変更は作業ブランチで実装し、コミットする前にユーザーがローカルで確認する。確認して OK と言われてからコミットし、PR を作る。PR のマージも毎回ユーザーに確認する。
- ローカル確認用サーバーは `/usr/bin/python3 dev/server.py` をバックグラウンドで起動する（キャッシュ禁止ヘッダー付き、0.0.0.0 に束縛して iPhone からも見られる）。アプリは `http://127.0.0.1:8765/public/` で開く。
- 外部ライブラリとサーバー通信は使わない。
- ユーザーとのやり取りは日本語で行う。
- 動作確認の補足: アプリ内ブラウザ（Claude Browser）ではサービスワーカーの登録がブロックされる。オフライン動作や更新の帯を確かめるときは Claude in Chrome で `http://localhost:8765/public/` を開き、終わったら登録とキャッシュを消してタブを閉じる。
- 動作確認で localStorage を書き換えたら、終わりに `katazuke.v1` を消してサンプルデータの状態に戻す。
- iPhone 実機の見え方は、ユーザーにスクリーンショットを貼ってもらって判断する。余白の実測値が必要なときは、一時的に `measureInsets()` の値を画面に出して確認する。
- コミット前に `./dev/check.sh` を実行して JS の構文と CSS の波括弧を確認する。
