# katazuke

部屋の片付けを続けるためのブラウザゲーム。素の HTML/CSS/JS、localStorage 保存、GitHub Pages で公開。

- 仕様は `SPEC.md`。仕様を変えるときはコードより先にここを直す。
- ブランチ運用とコミットメッセージの形式は `CONTRIBUTING.md` に従う。
- 変更は作業ブランチで実装し、コミットする前にユーザーがローカルで確認する。確認して OK と言われてからコミットし、PR を作る。PR のマージも毎回ユーザーに確認する。
- ローカル確認用サーバーは `/usr/bin/python3 -m http.server 8765 --bind 0.0.0.0` をバックグラウンドで起動する（iPhone からも見られるように 0.0.0.0 に束縛する）。
- 外部ライブラリとサーバー通信は使わない。
- ユーザーとのやり取りは日本語で行う。
- 動作確認の補足: アプリ内ブラウザ（Claude Browser）ではサービスワーカーの登録がブロックされる。オフライン動作や更新の帯を確かめるときは Claude in Chrome で `http://localhost:8765/` を開き、終わったら登録とキャッシュを消してタブを閉じる。
- 動作確認で localStorage を書き換えたら、終わりに `katazuke.v1` を消してサンプルデータの状態に戻す。
- iPhone 実機の見え方は、ユーザーにスクリーンショットと設定画面の「画面 …」の1行を貼ってもらって判断する。
